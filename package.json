{
  "name": "monorepo-yarn",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "packageManager": "yarn@3.6.3",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "dev": "yarn workspace @main/front vite",
    "build": "yarn workspace @main/front tsc && vite build",
    "preview": "yarn workspace @main/front vite preview"
  },
  "devDependencies": {
    "eslint": "^8.48.0",
    "prettier": "^3.0.3",
    "typescript": "^5.2.2"
  }
}
